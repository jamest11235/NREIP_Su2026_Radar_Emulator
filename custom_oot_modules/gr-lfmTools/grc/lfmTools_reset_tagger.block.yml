id: lfmTools_reset_tagger
label: Reset Tagger
category: '[LFM Tools]'

templates:
  imports: from gnuradio import lfmTools
  make: lfmTools.reset_tagger(${wait})

parameters:
- id: wait
  label: Wait Samples
  dtype: int
  default: 0

inputs:
- label: in
  domain: stream
  dtype: complex
- label: reset
  domain: message

outputs:
- label: out
  domain: stream
  dtype: complex

file_format: 1
